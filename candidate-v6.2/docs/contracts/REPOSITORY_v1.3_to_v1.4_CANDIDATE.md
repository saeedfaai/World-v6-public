# Repository Isolation v1.4 Candidate Overlay
Keep the generic deterministic projector in candidate Core. Entity-owned
mappings are versioned JSON Profile artifacts loaded by runtime, not duplicated
Python constants. Network/storage adapters live outside Entity private code and
are callable only by Core Executor. Do not create a distributed Resolution
service until measured need requires it. No empty scaffolding.
